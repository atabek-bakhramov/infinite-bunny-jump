// Since we're loading script at index.html at the very beginning
// our window object has not only built-in properties and methods,
// but now also all what Phaser library has (?)

export default window.Phaser;
